# Model Y 2026 焕新版试运行语料

状态：`TEST_ONLY`。本目录由用户于 2026-07-22 授权导入，仅用于验证 PDF/静态 HTML 的离线构建、云端 Embedding、Android 安装和检索链路。

唯一车辆 Scope：`MODEL_Y / 2026 / CN / 2026_REFRESH / RWD`，Scope ID 为 `model-y-2026-cn-2026-refresh-rwd`。

资料尚不完整，不得据此宣称完整车型知识库、锁定真实阈值或发布正式 Bundle。PDF 的车型归属来自用户确认，后续正式发布前仍须完成 Metadata 人工复核。

`corpus_html_pilot.json` 是特意缩减的 `TEST_ONLY` 构建输入：当前 Owner's Manual PDF 被 V1 解析质量门拒绝时，仅用可稳定解析的静态 HTML 验证端到端链路。它绝不能被表述为完整车辆手册知识库。
